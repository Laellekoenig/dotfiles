# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 30
inner.vertical =   30
outer.left =       [{monitor."C34J79x" = 800 }, {monitor."Studio Display" = 500}, {monitor."LG HDR 4K" = 450}, {monitor."Odyssey G50SF" = 450}, 30]
outer.bottom =     [{monitor."LG HDR 4K" = 100}, {monitor."Odyssey G50SF" = 50}, 30]
outer.top =        [{monitor."LG HDR 4K" = 100}, {monitor."Odyssey G50SF" = 50}, 30]
outer.right =       [{monitor."C34J79x" = 800 }, {monitor."Studio Display" = 500}, {monitor."LG HDR 4K" = 450}, {monitor."Odyssey G50SF" = 450}, 30]

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
#alt-shift-j = 'move down'
#alt-shift-k = 'move up'
alt-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
# alt-shift-j/k are used below for moving windows between vertical workspaces.
# alt-shift-j = 'resize smart -50'
# alt-shift-k = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
# Main/surface workspaces
ctrl-u = 'workspace 1'
ctrl-i = 'workspace 2'
ctrl-o = 'workspace 3'
ctrl-p = 'workspace 4'
ctrl-y = 'workspace 5'

# Extra/vertical workspaces
ctrl-shift-u = 'workspace 1x'
ctrl-shift-i = 'workspace 2x'
ctrl-shift-o = 'workspace 3x'
ctrl-shift-p = 'workspace 4x'

# Move down from the surface row to the matching extra workspace
ctrl-j = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1) aerospace workspace 1x ;;
    2) aerospace workspace 2x ;;
    3) aerospace workspace 3x ;;
    4) aerospace workspace 4x ;;
    1x|2x|3x|4x) ;;
esac'
'''

# Move up from an extra workspace to its surface workspace
ctrl-k = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) aerospace workspace 1 ;;
    2x) aerospace workspace 2 ;;
    3x) aerospace workspace 3 ;;
    4x) aerospace workspace 4 ;;
    1|2|3|4) ;;
esac'
'''

# Move right on the surface carousel; from extra workspaces, surface first
ctrl-m = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) ws=1 ;;
    2x) ws=2 ;;
    3x) ws=3 ;;
    4x) ws=4 ;;
esac

case "$ws" in
    1) aerospace workspace 2 ;;
    2) aerospace workspace 3 ;;
    3) aerospace workspace 4 ;;
    4) aerospace workspace 1 ;;
    5) aerospace workspace 1 ;;
esac'
'''

# Move left on the surface carousel; from extra workspaces, surface first
ctrl-n = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) ws=1 ;;
    2x) ws=2 ;;
    3x) ws=3 ;;
    4x) ws=4 ;;
esac

case "$ws" in
    1) aerospace workspace 4 ;;
    2) aerospace workspace 1 ;;
    3) aerospace workspace 2 ;;
    4) aerospace workspace 3 ;;
    5) aerospace workspace 4 ;;
esac'
'''

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
# Move focused window to main/surface workspaces
alt-shift-u = 'move-node-to-workspace 1'
alt-shift-i = 'move-node-to-workspace 2'
alt-shift-o = 'move-node-to-workspace 3'
alt-shift-p = 'move-node-to-workspace 4'

# Move focused window down from surface row to matching extra workspace
alt-shift-j = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1) aerospace move-node-to-workspace 1x ;;
    2) aerospace move-node-to-workspace 2x ;;
    3) aerospace move-node-to-workspace 3x ;;
    4) aerospace move-node-to-workspace 4x ;;
    1x|2x|3x|4x) ;;
esac'
'''

# Move focused window up from extra workspace to matching surface workspace
alt-shift-k = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) aerospace move-node-to-workspace 1 ;;
    2x) aerospace move-node-to-workspace 2 ;;
    3x) aerospace move-node-to-workspace 3 ;;
    4x) aerospace move-node-to-workspace 4 ;;
    1|2|3|4) ;;
esac'
'''

# Move focused window right on the surface carousel; from extra workspaces, surface first
alt-shift-m = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) ws=1 ;;
    2x) ws=2 ;;
    3x) ws=3 ;;
    4x) ws=4 ;;
esac

case "$ws" in
    1) aerospace move-node-to-workspace 2 ;;
    2) aerospace move-node-to-workspace 3 ;;
    3) aerospace move-node-to-workspace 4 ;;
    4) aerospace move-node-to-workspace 1 ;;
esac'
'''

# Move focused window left on the surface carousel; from extra workspaces, surface first
alt-shift-n = '''exec-and-forget bash -c '
ws="$(aerospace list-workspaces --focused)"
case "$ws" in
    1x) ws=1 ;;
    2x) ws=2 ;;
    3x) ws=3 ;;
    4x) ws=4 ;;
esac

case "$ws" in
    1) aerospace move-node-to-workspace 4 ;;
    2) aerospace move-node-to-workspace 1 ;;
    3) aerospace move-node-to-workspace 2 ;;
    4) aerospace move-node-to-workspace 3 ;;
esac'
'''

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-c = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

[workspace-to-monitor-force-assignment]
1 = 1
2 = 1
3 = 2
4 = 2
1x = 1
2x = 1
3x = 2
4x = 2
